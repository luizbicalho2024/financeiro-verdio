# Exemplo. Não envie credenciais reais para o GitHub.
#
# O Financeiro e o Simulador usam o mesmo cluster MongoDB.
# A identidade é compartilhada em simulador_db.users.
# Os dados financeiros continuam isolados em financeiro_verdio.

MONGO_CONNECTION_STRING = "mongodb+srv://USUARIO:SENHA@CLUSTER.mongodb.net/?retryWrites=true&w=majority"

# Banco exclusivo dos dados financeiros.
FINANCEIRO_MONGO_DB = "financeiro_verdio"

# Banco de identidade do Simulador. Pode ser omitido, pois "simulador_db" é o padrão.
IDENTITY_MONGO_DB = "simulador_db"

# Não são mais necessários:
# FINANCEIRO_ADMIN_EMAIL
# FINANCEIRO_ADMIN_PASSWORD
# [service_account]
# [firebase]
